# MCP tools (V1)

Phase 3 will register read-only tools such as `get_budget_snapshot`, `get_category_balances`, `simulate_debt_payoff`, and others described in [initial_plan.md](planning/initial_plan.md).

Until the MCP server ships, use the **CLI** (`ledgermind snapshot`, etc.) for the same underlying data.
